# task-dashboard-backend — контекст для Claude Code

## Що це
Спільний бекенд для кількох незалежних панелей IkorkaShop:
`task-dashboard` (проєкти/задачі), `ikorka-sysadmin` (техніка/активи/
паролі/задачі сисадміна), `ikorka-luiza` (особиста панель власниці).
`ikorka-coo` — навмисно НЕ тут, окремий сервіс/база (`ikorka-coo-backend`).

## Стек
- Node.js + Express (`server.js`), PostgreSQL через `pg`
- Деплой: Railway, автодеплой при пуші в `main`
- Схема: `schema.sql` — виконується вручну в Railway → Postgres → Data,
  НЕ автоматично при деплої

## Авторизація
PIN у заголовку `x-pin` → роль через `resolveRole`. Ролі:
`owner`, `manager`, `sysadmin`, `evgeniya`. Хто що бачить — дивись
`requireRole(...)` на кожному роуті; `credentials` (паролі) — тільки
`owner`+`sysadmin`, навіть `evgeniya` без доступу.

## Стійкість до помилок
Усі `app.get/post/patch/delete` обгорнуті (на самому початку файлу) так,
що кинута помилка з async-хендлера йде в `next(err)` замість того, щоб
зависнути без відповіді. Останній `app.use((err,req,res,next)=>...)` в
кінці файлу ловить це й повертає нормальний JSON 500 з CORS-заголовками.
Без цього невдалий запит (напр. відсутня таблиця) висів до таймауту
Railway, який підставляв власну fallback-відповідь без CORS —
браузер бачив це як "Failed to fetch", і оскільки кілька вкладок
вантажаться через `Promise.all`, одна зламана таблиця валила весь
дашборд. **Якщо додаєш новий роут — цей захист вже працює автоматично,
нічого додатково обгортати не треба.**

## Верифікація змін БЕЗ засмічення equipment_log
Коли перевіряєш новий/змінений код прямими curl-запитами до прод-API
(а не через реальний фронт) — і викликаєш `POST/PATCH/DELETE /api/equipment*`
— додай заголовок `x-claude-verify: 1`. Це не мокає й не блокує сам
запис (мутація в БД реальна, поведінку перевіряєш чесно), але сервер
пропускає автоматичний запис у `equipment_log` (created/status_changed/
zone_changed/deleted/inventory_check), який інакше засмічує "Тижневу
аналітику" фейковими діями і плутається з реальною роботою сисадміна.
Реальний фронт цей заголовок ніколи не надсилає.

Приклад:
```bash
curl -X PATCH -H "x-pin: $PIN" -H "x-claude-verify: 1" \
  -H "Content-Type: application/json" -d '{"status":"working"}' \
  "$API/equipment/123"
```

**Якщо перевіряєш кліками через живий сайт (не curl)** — заголовок сам
собою туди не потрапить, бо реальний фронт (`ikorka-sysadmin`) його
ніколи не додає. Замість цього постав прапорець у sessionStorage ДО
кліків:
```js
sessionStorage.setItem('ikorka_claude_verify', '1')
```
`src/api.js` цього репозиторію бачить його й сам додає заголовок до
кожного запиту з тієї вкладки. Прибери прапорець (`removeItem`) після
перевірки. Без цього кроку одна ж перевірка кліком (напр. видалення
через кнопку в рядку) пише в `equipment_log` як звичайна дія — це вже
траплялось (id 617, `TEST UI check item`).

**Правило на майбутнє**: якщо додаєш НОВИЙ роут/дію, яка сама пише в
`equipment_log`, — одразу обгортай той INSERT в `if (!skipLog(req))`
(або `if (<умова> && !skipLog(req))`, як для status/zone), за тим самим
патерном, що і в існуючих п'яти місцях (created, inventory_check,
status_changed, zone_changed, deleted). Не покладайся на те, що
"якось саме спрацює" — перевіряй це явно в діффі перед пушем.

Це стосується лише `equipment_log`. Для інших таблиць (daily_tasks,
assigned_tasks, assets, credentials, projects) при тестуванні — як і
раніше, створюй одноразовий запис із чітким префіксом (`TEST ...`),
перевіряй на ньому, видаляй одразу після. Ніколи не змінюй статус/поля
вже існуючих реальних записів "туди-сюди" для перевірки — навіть якщо
повертаєш значення назад, побічні ефекти (created_at змінних логів
тощо) можуть лишитись.

## Таймтрекінг assigned_tasks / luiza_assigned_tasks (пауза/відновлення)
Статуси: `queued → active → paused → active → ... → done` (пауза й
відновлення можна проходити скільки завгодно разів).
- `started_at` — коли задачу взяли в роботу ВПЕРШЕ. Виставляється один
  раз, ніколи не чіпається паузою/відновленням — саме це поле показує й
  дає бекдейтити UI («Взято: <дата>»).
- `session_started_at` — початок ПОТОЧНОЇ сесії роботи (`NULL`, поки
  задача не `active`). Виставляється в `now()` на кожному переході в
  `active` (і перший старт, і кожне відновлення після паузи).
- `accumulated_seconds` — сума вже завершених сесій. На паузу/завершення
  поточна сесія (`now() - session_started_at`) додається сюди, і
  `session_started_at` обнуляється.
Живий таймер (поки `active`) = `accumulated_seconds + (now - session_started_at)`.
На паузі показуємо просто `accumulated_seconds`. Уся ця логіка — в JS у
PATCH-хендлері (SELECT поточного рядка, потім UPDATE), НЕ в SQL CASE, бо
потрібен старий `status`, а не тільки старі таймстемпи. Явний бекдейтинг
`started_at`/`finished_at` (луіза-панель, `<input type="date">`) завжди
має пріоритет над обчисленим значенням і не чіпає `accumulated_seconds`.

## Файлові вкладення (attachments) — окремо від report_images
`report_images` (скріншоти) — base64 прямо в Postgres, свідомий
компроміс для маленьких стиснутих картинок. Довільні файли (PDF/docx/
xlsx тощо) так зберігати не можна — роздує базу. Замість цього:
- `POST /api/upload` (multipart, поле `file`, роль з `sysadminRoles`)
  зберігає файл на диску під `UPLOAD_DIR` і повертає `{url, name, size}`.
- `GET /api/uploads/:filename` віддає файл; авторизація через
  `?pin=`, бо `<a href download>` не може слати заголовок `x-pin` —
  той самий компроміс, що й у "Паролях" (внутрішній інструмент, PIN і
  так усюди в plaintext).
- Ці `{url, name, size}` кладуться в колонку `attachments` (JSONB-масив)
  у `assigned_tasks`/`luiza_assigned_tasks` через звичайний PATCH.

**КРИТИЧНО:** `UPLOAD_DIR` (за замовчуванням `./uploads` у корені
репо) мусить бути на **Railway Volume**, змонтованому в сервіс
`task-dashboard-backend` — інакше файли зникають при кожному деплої
(файлова система Railway поза volume ефемерна). Налаштування (робиться
вручну, не з коду):
1. Railway → сервіс `task-dashboard-backend` → Settings → Volumes →
   Add Volume, mount path напр. `/data/uploads`.
2. Variables → додати `UPLOAD_DIR=/data/uploads`.
Без цих двох кроків ендпоінт технічно працює (пише у звичайну
файлову систему контейнера), але вкладення губляться при наступному
деплої.

## Важливо при змінах
- Зміни в `schema.sql` НЕ застосовуються автоматично.
- Не хардкодити PIN або DATABASE_URL — тільки `process.env`.
- `ikorka-coo`/`ikorka-coo-backend` — окрема інфраструктура, не чіпати
  звідси.
